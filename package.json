{
  "name": "diva-cards-astro",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy:firebase": "npm run build && firebase deploy --only hosting,firestore:rules,firestore:indexes"
  },
  "dependencies": {
    "astro": "^5.0.0",
    "firebase": "12.17.1"
  },
  "devDependencies": {
    "firebase-tools": "^15.27.0"
  }
}
